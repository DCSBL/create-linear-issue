# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
repos:
  - repo: local
    hooks:
      - id: build-and-stage-dist
        name: Build
        entry: bash -c "npm run build"
        language: system
        files: ^src/.*\.ts$
        pass_filenames: false
